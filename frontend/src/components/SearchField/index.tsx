import React from 'react';
import SearchTextField from "@material-ui/core/TextField";

export const SearchField = () => {
    return (
        <SearchTextField
            variant='outlined'
            label='Search'
        />
    );
};